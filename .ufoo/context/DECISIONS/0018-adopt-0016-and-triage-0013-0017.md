---
status: resolved
resolved_by: codex
resolved_at: 2026-01-28
---
# DECISION 0018: Adopt 0016 (ai-workspace Umbrella) and Triage 0013/0017

Date: 2026-01-28
Author: Human / AI (Codex)

## Context

现有 open decisions：
- 0013：在 `ai-context` 内引入扩展机制并拆分资源（extensions.yaml）
- 0016：采用 `ai-workspace` umbrella + `ai-context`/`ai-resources` 模块化（全局 `~/.ai-workspace` + 项目 init 生成 `.ai-context/`）
- 0017：对 0016 的 review（建议分阶段）

用户明确偏好：**喜欢 0016 的方式**（直接 umbrella，按最佳实践，不做兼容）。

## Decision

1. **采纳 0016** 作为目标架构与实施方向。
2. **0017 作为 review 已完成**：不采纳其“分阶段先不改入口”的建议，直接以 0016 为准。
3. **0013 标记为 wontfix**：其“在 ai-context 内做 extensions.yaml 扩展机制”不再作为主路线（被 0016 替代）。

## Implications

- 0016 仍保持 open，代表“需要推进实现”的工作队列。
- 后续所有实现/拆分/脚本改造应围绕 0016 展开，并在实现过程写入新的 decisions（如仓库拆分、路径规范、init/doctor/skills 命令面等）。

